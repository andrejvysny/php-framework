<section id="userPanel">

    <header>
        <div class="avatar">
            <img src="{{ asset('assets/noimg.png') }}" class="img-fluid" alt="{{ app.user.username }}" />
        </div>

        <div class="user-info">
            <h3>@{{ app.user.username }}</h3>
            <p>{{ app.user.email }}</p>
            <a href="{{ path('security_logout') }}" class="btn btn-primary btn-sm">Odhlásiť sa</a>
        </div>

    </header>
    <div class="mt-3 text-center">
        <a href="{{ path('security_account_password_change') }}" class="btn btn-primary">Zmeniť heslo</a>
    </div>
</section>

<div id="overLayer"></div>